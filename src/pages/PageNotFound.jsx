import React from "react";

/**
 * Renders a page not found error view.
 * @returns {JSX.Element} A main element with a "Page Not Found" heading.
 * @description Displays a simple error page when a user navigates to a non-existent route in the application.
 */
export default function PageNotFound() {
  return (
    <main>
      <h1>Page Not Found</h1>
    </main>
  );
}